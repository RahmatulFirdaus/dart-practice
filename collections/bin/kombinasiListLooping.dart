void main(){
  List makanan = ["Mie Goreng", "Nasi Goreng", "Kwetiau Goreng"];
  print(makanan.length); // untuk menampilkan total data yang ada di list makanan
  for(int x = 0; x <= 2; x++){
    print(makanan[x]);
  }
  
}